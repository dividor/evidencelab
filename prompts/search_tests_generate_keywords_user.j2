Question: "{{ question }}"

Extract 3-5 disparate search keyword strings from the question above.
These should be terms a user might type into a search bar to find an answer to this question.
Output strictly as a JSON list of strings.

Example:
["keyword one", "keyword two", "complex phrase"]

Output:
