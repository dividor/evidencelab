Given the user's question, decompose it into 1-3 focused search queries that will help find relevant documents in the database. Each query should target a different aspect of the question.

User question: {{ query }}

{% if conversation_context %}
Previous conversation context:
{{ conversation_context }}
{% endif %}

Return a JSON array of search query strings. Each query should be:
- Specific enough to find relevant documents
- Use key terms and synonyms that might appear in evaluation reports
- Cover different angles of the question

Example output format:
["search query 1", "search query 2", "search query 3"]

Return ONLY the JSON array, no other text.
